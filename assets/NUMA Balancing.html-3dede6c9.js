import{_ as s}from"./plugin-vue_export-helper-c27b6911.js";import{r as t,o as i,c as o,a as e,b as a,d as c,e as r}from"./app-ab3a1edc.js";const d={},m={id:"numa-balancing-in-kernel-doc",tabindex:"-1"},l=e("a",{class:"header-anchor",href:"#numa-balancing-in-kernel-doc","aria-hidden":"true"},"#",-1),h={href:"https://www.kernel.org/doc/Documentation/sysctl/kernel.txt",target:"_blank",rel:"noopener noreferrer"},p=r("<p>Enables/disables automatic page fault based NUMA memory balancing. Memory is moved automatically to nodes that access it often.</p><p>Enables/disables automatic NUMA memory balancing. On NUMA machines, there is a performance penalty if remote memory is accessed by a CPU. When this feature is enabled the kernel samples what task thread is accessing memory by periodically unmapping pages and later trapping a page fault. At the time of the page fault, it is determined if the data being accessed should be migrated to a local memory node.</p><p>The unmapping of pages and trapping faults incur additional overhead that ideally is offset by improved memory locality but there is no universal guarantee. If the target workload is already bound to NUMA nodes then this feature should be disabled. Otherwise, if the system overhead from the feature is too high then the rate the kernel samples for NUMA hinting faults may be controlled by the numa_balancing_scan_period_min_ms, numa_balancing_scan_delay_ms, numa_balancing_scan_period_max_ms, numa_balancing_scan_size_mb, and numa_balancing_settle_count sysctls.</p><hr><blockquote><p>numa_balancing_scan_period_min_ms, numa_balancing_scan_delay_ms, numa_balancing_scan_period_max_ms, numa_balancing_scan_size_mb</p></blockquote><p>Automatic NUMA balancing scans tasks address space and unmaps pages to detect if pages are properly placed or if the data should be migrated to a memory node local to where the task is running. Every <code>scan delay</code> the task scans the next <code>scan size</code> number of pages in its address space. When the end of the address space is reached the scanner restarts from the beginning.</p><p>In combination, the <code>scan delay</code> and <code>scan size</code> determine the scan rate. When <code>scan delay</code> decreases, the scan rate increases. The scan delay and hence the scan rate of every task is adaptive and depends on historical behaviour. If pages are properly placed then the scan delay increases, otherwise the scan delay decreases. The <code>scan size</code> is not adaptive but the higher the <code>scan size</code>, the higher the scan rate.</p><p>Higher scan rates incur higher system overhead as page faults must be trapped and potentially data must be migrated. However, the higher the scan rate, the more quickly a tasks memory is migrated to a local node if the workload pattern changes and minimises performance impact due to remote memory accesses. These sysctls control the thresholds for scan delays and the number of pages scanned.</p><p>numa_balancing_scan_period_min_ms is the minimum time in milliseconds to scan a tasks virtual memory. It effectively controls the maximum scanning rate for each task.</p><p>numa_balancing_scan_delay_ms is the starting <code>scan delay</code> used for a task when it initially forks.</p><p>numa_balancing_scan_period_max_ms is the maximum time in milliseconds to scan a tasks virtual memory. It effectively controls the minimum scanning rate for each task.</p><p>numa_balancing_scan_size_mb is how many megabytes worth of pages are scanned for a given scan.</p>",12);function _(u,g){const n=t("ExternalLinkIcon");return i(),o("div",null,[e("h2",m,[l,a(),e("a",h,[a("numa_balancing in kernel doc"),c(n)])]),p])}const y=s(d,[["render",_],["__file","NUMA Balancing.html.vue"]]);export{y as default};
