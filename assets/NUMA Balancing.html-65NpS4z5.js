import{_ as s}from"./plugin-vue_export-helper-x3n3nnut.js";import{r as t,o as r,c as i,a as e,b as a,d as o,e as c}from"./app-YIMH3Z_5.js";const d={},m={id:"numa-balancing-in-kernel-doc",tabindex:"-1"},l=e("a",{class:"header-anchor",href:"#numa-balancing-in-kernel-doc","aria-hidden":"true"},"#",-1),h={href:"https://www.kernel.org/doc/Documentation/sysctl/kernel.txt",target:"_blank",rel:"noopener noreferrer"},b=c("<p>Enables/disables automatic page fault based NUMA memory<br> balancing. Memory is moved automatically to nodes<br> that access it often.</p><p>Enables/disables automatic NUMA memory balancing. On NUMA machines, there<br> is a performance penalty if remote memory is accessed by a CPU. When this<br> feature is enabled the kernel samples what task thread is accessing memory<br> by periodically unmapping pages and later trapping a page fault. At the<br> time of the page fault, it is determined if the data being accessed should<br> be migrated to a local memory node.</p><p>The unmapping of pages and trapping faults incur additional overhead that<br> ideally is offset by improved memory locality but there is no universal<br> guarantee. If the target workload is already bound to NUMA nodes then this<br> feature should be disabled. Otherwise, if the system overhead from the<br> feature is too high then the rate the kernel samples for NUMA hinting<br> faults may be controlled by the numa_balancing_scan_period_min_ms,<br> numa_balancing_scan_delay_ms, numa_balancing_scan_period_max_ms,<br> numa_balancing_scan_size_mb, and numa_balancing_settle_count sysctls.</p><hr><blockquote><p>numa_balancing_scan_period_min_ms, numa_balancing_scan_delay_ms,<br> numa_balancing_scan_period_max_ms, numa_balancing_scan_size_mb</p></blockquote><p>Automatic NUMA balancing scans tasks address space and unmaps pages to<br> detect if pages are properly placed or if the data should be migrated to a<br> memory node local to where the task is running. Every <code>scan delay</code> the task<br> scans the next <code>scan size</code> number of pages in its address space. When the<br> end of the address space is reached the scanner restarts from the beginning.</p><p>In combination, the <code>scan delay</code> and <code>scan size</code> determine the scan rate.<br> When <code>scan delay</code> decreases, the scan rate increases. The scan delay and<br> hence the scan rate of every task is adaptive and depends on historical<br> behaviour. If pages are properly placed then the scan delay increases,<br> otherwise the scan delay decreases. The <code>scan size</code> is not adaptive but<br> the higher the <code>scan size</code>, the higher the scan rate.</p><p>Higher scan rates incur higher system overhead as page faults must be<br> trapped and potentially data must be migrated. However, the higher the scan<br> rate, the more quickly a tasks memory is migrated to a local node if the<br> workload pattern changes and minimises performance impact due to remote<br> memory accesses. These sysctls control the thresholds for scan delays and<br> the number of pages scanned.</p><p>numa_balancing_scan_period_min_ms is the minimum time in milliseconds to<br> scan a tasks virtual memory. It effectively controls the maximum scanning<br> rate for each task.</p><p>numa_balancing_scan_delay_ms is the starting <code>scan delay</code> used for a task<br> when it initially forks.</p><p>numa_balancing_scan_period_max_ms is the maximum time in milliseconds to<br> scan a tasks virtual memory. It effectively controls the minimum scanning<br> rate for each task.</p><p>numa_balancing_scan_size_mb is how many megabytes worth of pages are<br> scanned for a given scan.</p>",12);function p(_,u){const n=t("ExternalLinkIcon");return r(),i("div",null,[e("h2",m,[l,a(),e("a",h,[a("numa_balancing in kernel doc"),o(n)])]),b])}const y=s(d,[["render",p],["__file","NUMA Balancing.html.vue"]]);export{y as default};
